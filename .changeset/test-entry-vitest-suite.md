---
"@vortex-api/convex-auth": minor
---

Ship a `@vortex-api/convex-auth/test` entry point exporting `convexAuthTest`, `modules`, and `schema` so consumers can register the component in their own `convex-test` harness, and wire the `*.vitest.ts` component conformance suite into CI coverage.
