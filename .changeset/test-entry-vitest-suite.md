---
"@vortex-api/convex-auth": minor
---

Ship a `@vortex-api/convex-auth/test` entry point — `register(t, name?)`, `convexAuthTest`, `modules`, and `schema` — so consumers can mount the component in their own `convex-test` harness, and wire the `*.vitest.ts` component conformance suite into CI coverage.
