---
"@vortex-api/convex-auth": minor
---

Add privileged `ConvexAdminDashboard` React component with super-admin authorization, plus Convex-native admin modules for users, sessions, organisations, and audit logs. Includes a `claimSuperAdmin` bootstrap mutation so the first eligible user can grant themselves admin access.
