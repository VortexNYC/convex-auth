---
"@vortex-api/convex-auth": minor
---

Add privileged `ConvexAdminDashboard` React component with super-admin authorization, plus Convex-native admin modules for users, sessions, organisations, and audit logs.
