---
"@vortex-api/convex-auth": minor
---

Add organization member and role management to `ConvexAdminDashboard`.

- List, search, and paginate organization members.
- Update member roles and remove members from the dashboard.
- Wire `listMembers`, `updateMemberRole`, and `removeMember` through the React example.
