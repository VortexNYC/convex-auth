---
"@vortex-api/convex-auth": patch
---

Bump internal dependencies: `@convex-dev/rate-limiter` 0.3 → 0.4 (additive release — `RateLimiter` constructor, `limit`, and `check` APIs unchanged; verified against the full rate-limit suite) and `convex-helpers` 0.1.80 → 0.1.124. Also bumps `@simplewebauthn/server`, `jose`, `oauth4webapi`, and `otpauth` to latest patches.
