export default function App() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold">Better Auth Migration Demo</h1>
      <p className="text-muted-foreground mt-2">
        Run the seed, then execute the migration CLI to move data into native convex-auth tables.
      </p>
    </div>
  );
}
