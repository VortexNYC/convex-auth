name: Secret scan

on:
  push:
    branches: [main]
  pull_request:

jobs:
  secretlint:
    name: secretlint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          run_install: true
      - run: pnpm run scan:secrets
